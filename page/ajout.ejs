<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Ajout / Modification</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <nav class="navbar navbar-dark bg-dark px-4">
    <div class="d-flex gap-3">
      <a href="/" class="text-white text-decoration-none">Accueil</a>
      <a href="/ajout" class="text-white text-decoration-none">Ajouter</a>
      <a href="/dashboard" class="text-white text-decoration-none">Dashboard</a>
    </div>
  </nav>

  <div class="container mt-4" style="max-width: 600px;">
    <h4 class="mb-4"><%= typeof tache !== 'undefined' ? 'Modifier la tâche' : 'Ajouter une tâche' %></h4>

    <div class="card p-4 shadow-sm">
      <form method="POST" action="<%= typeof tache !== 'undefined' ? '/modifier/' + tache.id : '/ajout' %>">

        <div class="mb-3">
          <label class="form-label fw-bold">Titre</label>
          <input type="text" name="titre" class="form-control" required
            value="<%= typeof tache !== 'undefined' ? tache.titre : '' %>">
        </div>

        <div class="mb-3">
          <label class="form-label fw-bold">Description</label>
          <textarea name="description" class="form-control" rows="3"><%= typeof tache !== 'undefined' ? tache.description : '' %></textarea>
        </div>

        <div class="mb-3">
          <label class="form-label fw-bold">Priorité</label>
          <select name="priorite" class="form-select">
            <option value="basse"   <%= (typeof tache !== 'undefined' && tache.priorite === 'basse')   ? 'selected' : '' %>>Basse</option>
            <option value="moyenne" <%= (typeof tache !== 'undefined' && tache.priorite === 'moyenne') ? 'selected' : '' %>>Moyenne</option>
            <option value="haute"   <%= (typeof tache !== 'undefined' && tache.priorite === 'haute')   ? 'selected' : '' %>>Haute</option>
          </select>
        </div>

        <div class="mb-3">
          <label class="form-label fw-bold">Date limite</label>
          <input type="date" name="date_limite" class="form-control"
            value="<%= typeof tache !== 'undefined' ? tache.date_limite.toISOString().split('T')[0] : '' %>">
        </div>

        <div class="mb-3">
          <label class="form-label fw-bold">Responsable</label>
          <input type="text" name="responsable" class="form-control" required
            value="<%= typeof tache !== 'undefined' ? tache.responsable : '' %>">
        </div>

        <button type="submit" class="btn btn-dark w-100">
          <%= typeof tache !== 'undefined' ? 'Enregistrer' : 'Ajouter' %>
        </button>

      </form>
    </div>
  </div>

</body>
</html>